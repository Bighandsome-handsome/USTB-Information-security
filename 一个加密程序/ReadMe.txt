闲的没事儿，这是一个加密和解密程序。
这个密码算法有三个秘钥需要你自己设定，秘钥为a，b，c。
如果你不确定自己的秘钥是否合理，你可以先在GetKey里测试。
GetKey：
输入a,b两个数值，给定c的一个范围，可以遍历出符合条件的a,b,c秘钥。
然后，你就可以使用这个算法了。
他可以把一段英文转化成若干个数字。
也可以将这段数字解密成英文。
界面设计还在优化当中，目前程序很基础。